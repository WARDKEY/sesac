// JsDoc
// 자바스클립트 소스를 .d.ts 파일을 생성할 때 사용

/**
 * 두 숫자를 더하는 함수
 * @param {number} a - 첫 번째 숫자
 * @param {number} b - 두 번째 숫자
 * @returns {number} - 두 숫자의 합
 */

export function add(a, b) {
  return a + b;
}
